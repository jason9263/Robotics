%home work 2
function [Prot] = rotateMat(R,p)

    sizeR = size(R);
    sizep = size(p);

    if(sizeR(1) == 3)
        

    Prot = R*p;
    %check the matrix R is the rotation matrix 
    %p is the 3 1 matrix
end
